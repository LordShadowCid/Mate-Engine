# LiveKit React client envs (Vite)
# Copy to `.env.local` and adjust for your setup

# Your local token server endpoint (from Tools/livekit-token-server)
VITE_TOKEN_ENDPOINT=http://localhost:8787/token

# Your LiveKit server URL (ws or wss)
VITE_LIVEKIT_URL=ws://localhost:7880

# Optional: default room name
VITE_ROOM=mate-dev
